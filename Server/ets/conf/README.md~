This file describe the meaning of each file in the conf directory

README.md
  This file

config.json.template
  This is the template for the configuration file of ETS. It should
  be copied as "config.json" by the install script. Do not modify the 
  .template file. Make your customization in config.js

ets-haproxy.conf
  This is the upstart file for haproxy. Copy it in /etc/init and
  customize it. Symlinking the file in /etc/init seems not to work.

ets-server.conf
  This is the upstart file for the ets server. Copy it in /etc/init and
  customize it. Symlinking the file in /etc/init seems not to work.

haproxy.conf.template
  This is the template for the configuration for ETS HAProxy. Do not
  modify the .template file. Instead copy it as haproxy.conf and customize it.
  This file is referenced in ets-haproxy.conf
  
monit.conf
  Not used at the moment. Contains some comments.

redis.conf
  Not used.